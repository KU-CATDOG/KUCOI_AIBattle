public enum TileType { Empty, Wall, Exit, Treasure, Sight }

public enum SightType { Empty, Invisible, Treasure, Thief }

public enum MoveDir { Up, Down, Left, Right, Neutral }

public enum MoveAngle { Up = 90, Down = 270, Left = 180, Right = 0, Null = -1 }
